<h2>Available drones</h2>
<ul>
{{#each drones}}
  <h3>{{name}}</h3>
    <li>Propellers: {{propellers}}</li>
    <li>Maximum Speed: {{maxSpeed}}</li>
    <a href="/drones/{{_id}}/edit">Edit this drone</a>
    <form action="/drones/{{_id}}/delete" method="POST">
      <button type="submit"> Delete </button>
    </form> 
{{/each}}
</ul>